version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install
  build:
    runtime-versions:
      nodejs: 10
    commands:
      - npm run build
artifacts:
  files:
    - build/*