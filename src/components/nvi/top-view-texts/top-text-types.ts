export enum NviTopTextViewVariant {
  Curator = 'curator',
  Admin = 'admin',
}
export interface PublicationPointsNumbers {
  totalCount: number;
  validPoints: number;
  percentageComparedToYearBefore: number;
  yearBefore: number;
}
