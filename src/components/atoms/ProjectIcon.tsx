import ShowChartIcon from '@mui/icons-material/ShowChart';

export const ProjectIcon = () => {
  return (
    <ShowChartIcon
      sx={{
        bgcolor: 'project.main',
        borderRadius: '0.4rem',
        color: 'black',
      }}
    />
  );
};
