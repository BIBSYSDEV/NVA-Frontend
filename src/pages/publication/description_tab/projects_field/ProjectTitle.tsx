import React, { FC } from 'react';
import { Typography } from '@material-ui/core';

const ProjectTitle: FC = ({ children }) => <Typography variant="subtitle1">{children}</Typography>;

export default ProjectTitle;
