// import { TextField } from '@material-ui/core';
// import React from 'react';
// import { IMaskMixin } from 'react-imask';

// type MaskedInputProps = {
//   unmask: boolean | 'typed';
//   onAccept: (value: string) => void;
// };

// const withMask = <P extends object>(Component: React.ComponentType<P>): React.FC<P & MaskedInputProps> =>
//   IMaskMixin(({ ...props }: P) => <Component {...props} />);

// const MaskedTextField = withMask(TextField);

// export const FieldMasked = ({ field: { onChange, ...fieldProps }, ...props }) => (
//   <MaskedTextField {...props} field={fieldProps} onChange={onChange} />
// );

// export default MaskedTextField;

export const a = 2;
