language: node_js
node_js:
  - 'stable'
env:
  global:
    - REACT_APP_USE_MOCK=true
cache:
  # Caches node_modules in all other cases
  npm: true
install:
  - npm ci
script:
  - npm test
  - npm run build
