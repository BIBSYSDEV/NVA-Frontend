version: 0.2

image: public.ecr.aws/docker/library/node:slim

env:
  variables:
    VITE_API_HOST: 'https://my-nva-api.no'

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm ci
  build:
    commands:
      - npm run build
