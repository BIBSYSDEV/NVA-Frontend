version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - env
      - npm ci
  build:
    commands:
      # - npm run deploy_prod
      # - npm run deploy_test
      - VERSION=v$(date +%Y%m%d)
      - BUILDS_BUCKET=frontend-builds-$(aws sts get-caller-identity --query Account --output text)
      - TO=s3://$BUILDS_BUCKET/builds/$VERSION
      - echo $TO
      - npm run deploy_sandbox
      - aws s3 mv /build ${TO}/sandbox --recursive
      # - aws s3 mv ${FROM}/prod ${TO}/prod --recursive
      # - aws s3 mv ${FROM}/test ${TO}/test --recursive
artifacts:
  files:
    - '**/*'
  base-directory: build
  name: nva-frontend
