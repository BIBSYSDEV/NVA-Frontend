version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm ci
  build:
    commands:
      - npm run build
      # - npm run test:react
      - npm run test:cypress --coverage
      - npx nyc report
